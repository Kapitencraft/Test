int x = randInt(-200, 200); //zufallszahlen zum testen
int y = randInt(-200, 200);
print(x + "," + y); //debug: input zahlen
int x1 = x; //Variablen kopieren um die ogs nicht zu verändern
int y1 = y;
int ergebnis = 0;
while (abs(x1) >= 1) {//(abs(x) = betrag(x))
    if (x1 % 2 == 1) ergebnis += y1; //x1 ist ungerade
    x1/=2; //teile x1 / 2 und stelle sicher, dass es immer ein int ist
    y1*=2; //multipliziere die zweite zahl mit 2
}
if (x < 0 and y < 0) ergebnis*=-1; //falls beide Zahlen negativ sind mache das ergebnis wieder positiv, weil es das an Anfang noch nicht ist
print(ergebnis); //berechnetes Ergebnis ausgeben
print(x*y); //debug: richtiges ergebnis